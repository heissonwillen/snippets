# ImageDraw.Draw(image).line(
#     xy=[(200, 400), (400, 400), (400, 600)],
#     fill=FILL,
#     width=1,
# )

# coordinates = [(int(image.size[0]/2 + cos(k*theta)*RADIUS), int(image.size[1]/2 + sin(k*theta)*RADIUS)) for k in range(-NAILS, NAILS)]

# radian_to_degree = lambda theta: (theta * 180) / pi
# degree_to_radian = lambda theta: (pi * theta) / 180
